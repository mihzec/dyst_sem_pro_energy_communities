name: energy.communities
services:
  database:
    image: postgres:alpine
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=disysuser
      - POSTGRES_PASSWORD=disyspw

  rabbitmq:
    image: rabbitmq:management-alpine
    ports:
      - '5672:5672'
      - '15672:15672'

  energy.server:
    image: energy.server:${APP_VERSION}
    ports:
      - '8080:8080'
    depends_on:
      - database
      - rabbitmq

  energy.producer:
    image: energy.producer:${APP_VERSION}
    ports:
      - '8081:8080'
    depends_on:
      - database
      - rabbitmq

  energy.user:
    image: energy.user:${APP_VERSION}
    ports:
      - '8082:8080'
    depends_on:
      - database
      - rabbitmq

  current.percentage.service:
    image: current.percentage.service:${APP_VERSION}
    ports:
      - '8083:8080'
    depends_on:
      - database
      - rabbitmq

  usage.service:
    image: usage.service:${APP_VERSION}
    ports:
      - '8084:8080'
    depends_on:
      - database
      - rabbitmq